# Open a new terminal window and execute "npm run dev:api"
gnome-terminal -- npm run dev:api &

# Open another terminal window and execute "npm run dev:web"
gnome-terminal -- npm run dev:web &

# Open another terminal window and execute "npm run dev:app"
# gnome-terminal -- npm run dev:app &
