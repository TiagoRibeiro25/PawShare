// import cities from 'cities.json';

// // TODO(tiago): Fix performance issue (cities.json is too big)
// function getCitiesFromCountry(country: string): string[] {
// 	return (
// 		cities
// 			//@ts-ignore
// 			.filter((city: any) => city.country === country)
// 			.map((city: any) => {
// 				return city.name;
// 			})
// 	);
// }

// export default { getCitiesFromCountry };
