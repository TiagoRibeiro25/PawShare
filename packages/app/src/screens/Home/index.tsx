import React from 'react';
import { Text, View } from 'react-native';

const Home: React.FC = (): React.JSX.Element => {
	return (
		<View>
			<Text>Home</Text>
		</View>
	);
};

export default Home;
